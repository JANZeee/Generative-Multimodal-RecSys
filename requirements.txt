# ===================================================================
#  Requirements for Generative Multimodal Recommendation System
#  These versions are chosen for stability and compatibility.
# ===================================================================

# --- Core Deep Learning Framework ---
# IMPORTANT: The torch version might depend on your CUDA installation.
# The versions below are a stable combination. For a different CUDA version,
# please visit https://pytorch.org/get-started/locally/ to find the correct command.
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2

# --- Hugging Face for Multimodal Models (CLIP) ---
transformers==4.35.2
accelerate==0.25.0

# --- Data Science and Machine Learning Stack ---
# NOTE: These versions are specifically chosen to be compatible with each other.
# numpy < 2.0 is critical to avoid binary incompatibility with other libraries.
numpy==1.26.4
pandas==1.5.3
scikit-learn==1.3.2
scipy==1.11.4

# --- Utilities and Helpers ---
tqdm==4.66.1          # For progress bars
requests==2.31.0      # For downloading images from URLs
Pillow==10.1.0        # For image processing (dependency of transformers)
matplotlib==3.8.2     # For plotting training curves

# --- Optional for Notebooks ---
jupyterlab==4.0.9
ipywidgets==8.1.1